## Supabase の選定理由

* **RLS（Row Level Security）によるセキュリティの確保**
* 初めてのWebアプリケーション開発でセキュリティ対策に不安がありました。
* SupabaseのRLSを使用することで、データベース側でアクセス制御を行い、セキュリティをある程度担保できるため採用しました。


* **研修環境へのスムーズな導入**
* 無料で使用でき、既存のGitHubアカウントでログインが可能です。
* 社内ツール等の導入制限がある研修環境下でも問題なく導入できました。



---

## GitHub Pages の選定理由

* **研修環境の制約（サーバー構築不可）の回避**
* 研修環境の制限上、新たに外部サーバーを立ち上げることが不可能な状態でした。


* **登録の手間削減と自動デプロイ**
* 研修で既にGitHubを使用していたため、新たにアカウント登録をする必要がありませんでした。
* リポジトリへ `git push` を行うだけで自動的にデプロイまで完了するため採用しました。


* **運用・管理コストの最小化**
* 個人開発であったため、複雑なツールの導入を避け、シンプルな構成で管理を楽にすることを優先しました。



---

## HTML / CSS / JavaScript の選定理由

* **GitHub Pagesの仕様（`index.html`）への対応**
* GitHub Pagesでホスティングを行うにあたり、`index.html` を使用することが必須要件でした。


* **標準技術によるシンプルなファイル連携**
* ベースとなるHTMLに対し、最も連携がスムーズで扱いやすいCSSおよびJavaScript（Pure JS）を用いて、スタイルや動的機能を構築しました。